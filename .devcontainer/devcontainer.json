{
  "name": "PHP 8.0 + MySQL",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "felixfbecker.php-debug",
        "bmewburn.vscode-intelephense-client"
      ]
    }
  },
  "forwardPorts": [8000],
  "postCreateCommand": "php -m | grep mysqli || echo '❌ mysqli still missing'"
}

